# Copie ce fichier en .env pour le développement local
# Sur Railway, configure ces variables dans le dashboard

PORT=3001
CLIENT_URL=http://localhost:5173

# PostgreSQL (fourni automatiquement par Railway si tu ajoutes un plugin Postgres)
DATABASE_URL=postgresql://user:password@localhost:5432/nop_db
